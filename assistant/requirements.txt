# Minimum versions only — same caveat as the narrative engine's
# requirements.txt: pin exact versions after your first successful install
# (`pip freeze > requirements.lock.txt`).
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
pypdf>=5.0
pydantic>=2.9
pydantic-settings>=2.6
asyncpg>=0.30
pgvector>=0.3
kuzu>=0.6
ollama>=0.4
anthropic>=0.40
openai>=1.55
python-dotenv>=1.0
pytest>=8.0
